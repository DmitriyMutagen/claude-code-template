# ═══════════════════════════════════════════
# Testing Dependencies — Elite Quality Stack
# Install: pip install -r requirements-test.txt
# ═══════════════════════════════════════════

# Core
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
pytest-mock>=3.12
pytest-xdist>=3.5              # parallel: pytest -n auto
pytest-timeout>=2.2            # prevent hanging tests
pytest-sugar>=1.0              # pretty output
pytest-randomly>=3.15          # random order (catch hidden deps)

# HTTP testing
httpx>=0.27                    # async HTTP client for API tests
respx>=0.22                    # mock httpx requests (WB/Ozon API)

# Data generation
factory-boy>=3.3               # SQLAlchemy model factories
Faker>=20.0                    # realistic fake data

# Property-based testing
hypothesis>=6.100              # "for ANY input, this property holds"

# API contract testing
schemathesis>=3.25             # auto-test ALL endpoints from OpenAPI spec

# AI/LLM quality testing
deepeval>=3.0                  # LLM output quality scoring + pytest integration

# Playwright (E2E)
playwright>=1.40
pytest-playwright>=0.5
